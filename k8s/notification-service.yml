apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: gadget-shop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: notification-service:latest
        imagePullPolicy: Never
        env:
        - name: HTTP_PORT
          value: "8086"
        - name: DB_DSN
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: NOTIFICATION_DB_DSN
        ports:
        - containerPort: 8086
        readinessProbe:
          httpGet:
            path: /health
            port: 8086
          initialDelaySeconds: 10
          periodSeconds: 15
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: gadget-shop
spec:
  selector:
    app: notification-service
  ports:
  - port: 8086
    targetPort: 8086
